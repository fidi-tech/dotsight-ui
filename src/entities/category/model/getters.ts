import {Category} from './types';

export const getCategoryId = (category: Category) => category.id;
export const getCategoryName = (category: Category) => category.name;
export const getCategoryIcon = (category: Category) => category.icon;